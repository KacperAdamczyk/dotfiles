README.md
Brewfile
packages-fedora.txt

{{ if or (ne .chezmoi.os "darwin") (not (dig "colima" "drive" "" .)) -}}
# Colima is macOS-only; its wrapper also requires an external drive.
.config/fish/functions/colima-start.fish
{{ end -}}

{{ if ne .chezmoi.os "darwin" -}}
# The managed block in ~/.ssh/config sets UseKeychain, an Apple-only keyword
# that makes ssh elsewhere exit with "Bad configuration option".
.ssh/config
# Linux manages its own container engine and Compose plugins.
.docker/config.json
{{ end -}}

{{ if not (dig "protonpass" "sshKeyItem" "" .) -}}
# No Proton Pass item configured on this machine, so there is nowhere to fetch
# the SSH key from — leave whatever key is already on disk alone.
.ssh/id_ed25519
.ssh/id_ed25519.pub
{{ end -}}
